{% extends 'base.html.twig' %}
{% import 'html.macro.twig' as html %}

{% block content %}
    <h1>Groep toevoegen</h1>
    {% if (form is defined) %}
        {{ form(form) }}
    {% else %}
        <p>Selecteer het type groep dat je wilt toevoegen:</p>
        <ul>
            {% for groupType,groupName in groupTypes %}
                <li>{{ html.link(groupName, path(route_base~'add', {type: groupType})) }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
