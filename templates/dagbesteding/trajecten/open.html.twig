{% extends 'base.html.twig' %}
{% import 'html.macro.twig' as html %}

{% block content %}
    <h1>Traject heropenen</h1>
    <p>
        Weet u zeker dat u traject
        {{ html.link(entity, path('dagbesteding_trajecten_view', {id: entity.id})) }}
        wilt heropenen?
    </p>
    {{ form(form) }}
{% endblock %}
