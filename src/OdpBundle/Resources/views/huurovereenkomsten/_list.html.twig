{% if huurder is defined %}
    {% set entity = huurder %}
    {% set entity_name = 'huurder' %}
{% elseif verhuurder is defined %}
    {% set entity = verhuurder %}
    {% set entity_name = 'verhuurder' %}
{% endif %}

<h2>Koppelingen</h2>
<table class="table">
    <tr>
        <th>{{ entity_name|capitalize }}</th>
        <th>Startdatum</th>
        <th>Opzegdatum</th>
        <th>Einddatum</th>
        <th>Afsluitdatum</th>
        <th></th>
    </tr>
    {% for huurovereenkomst in entity.huurovereenkomsten %}
        <tr>
            <td>{{ entity_name == 'huurder' ? huurovereenkomst.verhuurder : huurovereenkomst.huurder }}</td>
            <td>
                {{ huurovereenkomst.startdatum ? huurovereenkomst.startdatum|date('d-m-Y') }}
            </td>
            <td>
                {{ huurovereenkomst.opzegdatum ? huurovereenkomst.opzegdatum|date('d-m-Y') }}
            </td>
            <td>
                {{ huurovereenkomst.einddatum ? huurovereenkomst.einddatum|date('d-m-Y') }}
            </td>
            <td>
                {{ huurovereenkomst.afsluitdatum ? huurovereenkomst.afsluitdatum|date('d-m-Y') }}
                {{ huurovereenkomst.afsluiting ? '(' ~ huurovereenkomst.afsluiting ~ ')' }}
            </td>
            <td>
                {{ html.viewlink(path('odp_huurovereenkomsten_view', {id: huurovereenkomst.id})) }}
            </td>
        </tr>
    {% endfor %}
</table>
