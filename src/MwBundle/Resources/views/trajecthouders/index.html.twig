{% extends 'base.html.twig' %}
{% import 'html.macro.twig' as html %}

{% block title %}
    Maatschappelijk werk | {{ title }}
{% endblock %}

{% block subnavigation %}
    {% include '@Mw/subnavigation.html.twig' %}
{% endblock %}

{% block content %}
    <h1>{{ title }}</h1>
    <p>
        {{ html.link(entity_name|capitalize ~ ' toevoegen', path(route_base~'add'), 'add') }}
    </p>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>
                    {{ knp_pagination_sortable(pagination, 'Naam', 'trajecthouder.naam') }}
                </th>
                <th>
                    {{ knp_pagination_sortable(pagination, 'Startdatum', 'trajecthouder.startdatum') }}
                </th>
                <th>
                    {{ knp_pagination_sortable(pagination, 'Einddatum', 'trajecthouder.einddatum') }}
                </th>
                <th>
                </th>
            </tr>
        </thead>
        <tbody>
            {% for trajecthouder in pagination %}
                <tr>
                    <td>
                        {{ trajecthouder.naam }}
                    </td>
                    <td>
                        {{ trajecthouder.startdatum|date('d-m-Y') }}
                    </td>
                    <td>
                        {{ trajecthouder.einddatum|if_date('d-m-Y') }}
                    </td>
                    <td>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    {{ knp_pagination_render(pagination) }}
{% endblock %}
