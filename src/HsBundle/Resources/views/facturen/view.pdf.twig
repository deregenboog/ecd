<h1 align="right">Homeservice Amsterdam</h1>
<p align="right">
    Derde Schinkelstraat 9<br>
    1075 TJ AMSTERDAM<br>
    Tel. (020) 626 0969<br>
    De Regenboog Groep<br>
</p>
<p style="font-weight: bold;">
    {{ entity.klant.naam }}<br>
    {{ entity.klant.adres }}<br>
    {{ entity.klant.postcode }} {{ entity.klant.plaats }}
</p>
<p style="font-weight: bold;">
    Betreft: {{ entity.betreft }}<br>
    Datum: {{ entity.datum|date('d-m-Y') }}
</p>
<p>
    Geachte klant,
</p>
<p>
    Hierbij ontvangt u een factuur voor de geleverde diensten van Homeservice.<br>
    Het gaat hierbij om het aantal gewerkte uren van de dienstverlener(s) plus eventuele overige gemaakte kosten.<br>
    Uit onze administratie blijkt dat u gebruik heeft gemaakt van de volgende diensten:
</p>
<p>
    <table>
        {% for registratie in entity.registraties %}
            <tr>
                <td width="15%">{{ registratie.datum|date('d-m-Y') }}</td>
                <td width="15%">{{ registratie.activiteit }}</td>
                <td width="30%">{{ registratie.arbeider }}</td>
                <td width="10%">{{ registratie.start|date('H:i') }}</td>
                <td width="10%">{{ registratie.eind|date('H:i') }}</td>
                <td width="10%" align="right">{{ registratie.uren }} à {{ 2.5|money }}</td>
                <td width="10%" align="right">{{ (2.5 * registratie.uren)|money }}</td>
            </tr>
        {% endfor %}
        {% for declaratie in entity.declaraties %}
            <tr>
                <td width="15%">{{ declaratie.datum|date('d-m-Y') }}</td>
                <td width="15%">{{ declaratie.declaratieCategorie }}</td>
                <td width="30%"></td>
                <td width="10%"></td>
                <td width="10%"></td>
                <td width="10%"></td>
                <td width="10%" align="right">{{ declaratie.bedrag|money }}</td>
            </tr>
        {% endfor %}
        <tr>
            <td colspan="7"></td>
        </tr>
        <tr>
            <td colspan="6"></td>
            <td align="right">{{ entity.bedrag|money }}</td>
        </tr>
    </table>
</p>
