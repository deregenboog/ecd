<h2>Memo's</h2>
<p>
    {% if klus is defined %}
        {% set entity = klus %}
        {% set url = path('hs_memos_add', {klus: entity.id, redirect: app.request.get('url')}) %}
    {% elseif klant is defined %}
        {% set entity = klant %}
        {% set url = path('hs_memos_add', {klant: entity.id, redirect: app.request.get('url')}) %}
    {% elseif dienstverlener is defined %}
        {% set entity = dienstverlener %}
        {% set url = path('hs_memos_add', {dienstverlener: entity.id, redirect: app.request.get('url')}) %}
    {% elseif vrijwilliger is defined %}
        {% set entity = vrijwilliger %}
        {% set url = path('hs_memos_add', {vrijwilliger: entity.id, redirect: app.request.get('url')}) %}
    {% endif %}
    {% if url is defined %}
        {{ html.addLink(url) }}
        {{ html.link('Memo toevoegen', url) }}
    {% endif %}
</p>
<table class="table table-hover">
    {% for memo in entity.memos %}
        <tr>
            <td>{{ memo.datum|date('d-m-Y') }}</td>
            <td>{{ memo.medewerker }}</td>
            <td>{{ memo.memo|nl2br }}</td>
            <td>
                {{ html.editLink(path('hs_memos_edit', {id: memo.id, redirect: app.request.get('url')})) }}
            </td>
        </tr>
    {% endfor %}
</table>
