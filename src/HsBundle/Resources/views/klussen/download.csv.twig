Klusnummer{{ tab -}}
Klant{{ tab -}}
Werkgebied{{ tab -}}
Wachttijd{{ tab -}}
Memo's{{ tab -}}
Status{{ tab -}}
Activiteit
{% for entity in collection -%}
    {{ entity.id ~ tab -}}
    {{ entity.klant ~ tab -}}
    {{ entity.klant.werkgebied ~ tab -}}
    {{ 'wachttijd' ~ tab -}}
    {% for memo in entity.memos -%}
        ({{ memo.datum|date('d-m-Y') }}) {{ memo.memo|nl2ws ~ " " -}}
    {% endfor %}{{ tab -}}
    {{ 'status' ~ tab -}}
    {{ entity.activiteit }}
{% endfor %}
