{% extends '::base-2-col.html.twig' %}
{% import '::html.macro.twig' as html %}

{% block title %}
    Er op uit | Vrijwilligers
{% endblock %}

{% block subnavigation %}
    {% include '@ErOpUit/subnavigation.html.twig' %}
{% endblock %}

{% block content_top %}
    <h1>Vrijwilliger {{ entity }}</h1>
{% endblock %}

{% block content_left %}
    {% include '::vrijwilliger_basis.html.twig' with {vrijwilliger: entity.vrijwilliger} %}
{% endblock %}

{% block content_right %}
    <h2>ErOpUit-kalender</h2>
    <p>
        {% if entity.uitschrijfdatum %}
            {{ html.link('Lidmaatschap activeren', path('ga_vrijwilligerlidmaatschappen_reopen', {id: entity.id}), 'open') }}
        {% else %}
            {{ html.link('Lidmaatschap wijzigen', path('ga_vrijwilligerlidmaatschappen_edit', {id: entity.id}), 'edit') }}
            {{ html.link('Lidmaatschap beëindigen', path('ga_vrijwilligerlidmaatschappen_close', {id: entity.id}), 'close') }}
        {% endif %}
    </p>
    <dl class="dl-horizontal">
        <dt>Inschrijfdatum</dt>
        <dd>{{ entity.inschrijfdatum|date('d-m-Y') }}</dd>
        <dt>Uitschrijfdatum</dt>
        <dd>{{ entity.uitschrijfdatum ? entity.uitschrijfdatum|date('d-m-Y') }}</dd>
        <dt>Uitschrijfreden</dt>
        <dd>{{ entity.uitschrijfreden }}</dd>
    </dl>
    <dl class="dl-horizontal">
        <dt>Communicatie e-mail</dt>
        <dd>{{ entity.communicatieEmail ? 'Ja' :'Nee' }}</dd>
        <dt>Communicatie post</dt>
        <dd>{{ entity.communicatiePost ? 'Ja' :'Nee' }}</dd>
        <dt>Communicatie telefoon</dt>
        <dd>{{ entity.communicatieTelefoon ? 'Ja' :'Nee' }}</dd>
    </dl>
{% endblock %}
