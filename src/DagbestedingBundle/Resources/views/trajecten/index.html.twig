{% extends '::base.html.twig' %}
{% import '::html.macro.twig' as html %}

{% block title %}
    Dagbesteding | {{ title }}
{% endblock %}

{% block subnavigation %}
    {% include '@Dagbesteding/subnavigation.html.twig' %}
{% endblock %}

{% block content %}
    <h1>{{ title }}</h1>
    <table class="table table-hover">
{#        <tr>
            {{ form_start(filter) }}
            <th>
                {{ form_widget(filter.id) }}
                {{ form_errors(filter.id) }}
            </th>
            <th>
                {{ form_widget(filter.klant.naam) }}
                {{ form_errors(filter.klant.naam) }}
            </th>
            <th>
                {{ form_widget(filter.klant.stadsdeel) }}
                {{ form_errors(filter.klant.stadsdeel) }}
            </th>
            <th>
                {{ form_widget(filter.startdatum.start) }}
                {{ form_errors(filter.startdatum.start) }}
                {{ form_widget(filter.startdatum.end) }}
                {{ form_errors(filter.startdatum.end) }}
            </th>
            <th>
                {{ form_widget(filter.afsluitdatum.start) }}
                {{ form_errors(filter.afsluitdatum.start) }}
                {{ form_widget(filter.afsluitdatum.end) }}
                {{ form_errors(filter.afsluitdatum.end) }}
            </th>
            <th>
                {{ form_widget(filter.filter) }}
                {{ form_widget(filter.download) }}
            </th>
            {{ form_end(filter) }}
        </tr>
#}        <tr>
            <th>
                {{ knp_pagination_sortable(pagination, '#', 'traject.id') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Deelnemer', 'klant.achternaam') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Startdatum', 'traject.startdatum') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Rapportagedata', 'rapportage.datum') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Geplande einddatum', 'traject.einddatum') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Resultaat', 'traject.resultaat') }}
            </th>
            <th>
            </th>
        </tr>
        {% for traject in pagination %}
        <tr>
            <td>
                {{ html.link(traject.id, path('dagbesteding_trajecten_view', {id: traject.id})) }}
            </td>
            <td>
                {{ traject.deelnemer.klant.naam }}
            </td>
            <td>
                {{ traject.startdatum|date('d-m-Y') }}
            </td>
            <td>
                {% for rapportage in traject.rapportages %}
                    {{ rapportage.datum|date('d-m-Y') }}
                {% endfor %}
            </td>
            <td>
                {{ traject.afsluitdatum ? traject.afsluitdatum | date('d-m-Y') }}
            </td>
            <td>
            </td>
            <td>
                {{ html.viewLink(path('dagbesteding_trajecten_view', {id: traject.id})) }}
                {{ html.editLink(path('dagbesteding_trajecten_edit', {id: traject.id})) }}
                {% if traject.deletable %}
                    {{ html.deleteLink(path('dagbesteding_trajecten_delete', {id: traject.id})) }}
                {% endif %}
            </td>
        </tr>
        {% endfor %}
    </table>

    <p>
        {{ knp_pagination_render(pagination) }}
    </p>
{% endblock %}
