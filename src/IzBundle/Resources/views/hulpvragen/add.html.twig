{% extends 'IzBundle:hulpvragen:view.html.twig' %}

{% block content_right %}
    <div class="tab-content">
        <div id="koppelingen" class="tab-pane active">
        <h2>Hulpvraag toevoegen</h2>
        <p>
            {{ html.cancelLink(path('cake_iz_hulpvragen_view', {iz_klant_id: entity.izKlant.id})) }}
            {{ html.link('Annuleren', path('cake_iz_hulpvragen_view', {iz_klant_id: entity.izKlant.id})) }}
        </p>
        {{ form(form) }}
        </div>
    </div>
{% endblock %}
