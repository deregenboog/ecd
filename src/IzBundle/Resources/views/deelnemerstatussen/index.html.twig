{% extends '::base.html.twig' %}
{% import '::html.macro.twig' as html %}

{% block title %}
    Informele Zorg | {{ title }}
{% endblock %}

{% block subnavigation %}
    {% include 'IzBundle::subnavigation.html.twig' %}
{% endblock %}

{% block content %}
    <h1>{{ title }}</h1>
    <p>
        {{ html.addLink(path(route_base~'add')) }}
        {{ html.link(entity_name|capitalize ~ ' toevoegen', path(route_base~'add')) }}
    </p>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>
                    {{ knp_pagination_sortable(pagination, 'Naam', 'koppelingstatus.naam') }}
                </th>
                <th>
                    {{ knp_pagination_sortable(pagination, 'Actief', 'koppelingstatus.actief') }}
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for koppelingstatus in pagination %}
            <tr>
                <td>
                    {{ koppelingstatus.naam }}
                </td>
                <td>
                    {{ koppelingstatus.actief ? 'Ja' : 'Nee' }}
                </td>
                <td>
                    {{ html.editLink(path(route_base~'edit', {id: koppelingstatus.id})) }}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {{ knp_pagination_render(pagination) }}
{% endblock %}
