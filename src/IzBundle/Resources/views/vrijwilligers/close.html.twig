{% extends 'IzBundle:vrijwilligers:view.html.twig' %}

{% block content_right %}
    {% include 'IzBundle:vrijwilligers:_tabs.html.twig' with {vrijwilliger: entity, current: 'afsluiting'} %}
    <div class="tab-content">
        <div id="afsluiting" class="tab-pane active">
            <h2>Afsluiting</h2>
            <p>
                {{ html.cancelLink(path('iz_vrijwilligers_view', {id: entity.id, _fragment: 'afsluiting'})) }}
                {{ html.link('Annuleren', path('iz_vrijwilligers_view', {id: entity.id, _fragment: 'afsluiting'})) }}
            </p>
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
