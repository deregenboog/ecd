{% extends '::base.html.twig' %}
{% import '::html.macro.twig' as html %}

{% block title %}
    Clip | {{ title }}
{% endblock %}

{% block subnavigation %}
    {% include '@Clip/subnavigation.html.twig' %}
{% endblock %}

{% block content %}
    <h1>{{ title }}</h1>
    <table class="table table-hover">
        <tr>
            {{ form_start(filter) }}
            <th>
                {{ form_widget(filter.id) }}
                {{ form_errors(filter.id) }}
            </th>
            <th>
                {{ form_widget(filter.client.klant.naam) }}
                {{ form_errors(filter.client.klant.naam) }}
            </th>
            <th>
                {{ form_widget(filter.soort) }}
                {{ form_errors(filter.soort) }}
            </th>
            <th>
                {{ form_widget(filter.medewerker) }}
                {{ form_errors(filter.medewerker) }}
            </th>
            <th>
                {{ form_widget(filter.startdatum.start) }}
                {{ form_errors(filter.startdatum.start) }}
                {{ form_widget(filter.startdatum.end) }}
                {{ form_errors(filter.startdatum.end) }}
            </th>
            <th>
                {{ form_widget(filter.filter) }}
                {{ form_widget(filter.download) }}
            </th>
            {{ form_end(filter) }}
        </tr>
        <tr>
            <th>
                {{ knp_pagination_sortable(pagination, '#', 'vraag.id') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Deelnemer', 'klant.achternaam') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Soort', 'vraagsoort.naam') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Medewerker', 'medewerker.voornaam') }}
            </th>
            <th>
                {{ knp_pagination_sortable(pagination, 'Startdatum', 'vraag.startdatum') }}
            </th>
            <th>
            </th>
        </tr>
        {% for vraag in pagination %}
        <tr>
            <td>
                {{ html.link(vraag.id, path('clip_vragen_view', {id: vraag.id})) }}
            </td>
            <td>
                {{ vraag.client.klant.naam }}
            </td>
            <td>
                {{ vraag.soort }}
            </td>
            <td>
                {{ vraag.medewerker }}
            </td>
            <td>
                {{ vraag.startdatum|date('d-m-Y') }}
            </td>
            <td>
                {{ html.viewLink(path('clip_vragen_view', {id: vraag.id})) }}
                {{ html.editLink(path('clip_vragen_edit', {id: vraag.id})) }}
            </td>
        </tr>
        {% endfor %}
    </table>

    <p>
        {{ knp_pagination_render(pagination) }}
    </p>
{% endblock %}
