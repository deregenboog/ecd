{% extends 'base.html.twig' %}
{% import 'html.macro.twig' as html %}

{% block content %}
    <h1>{{ entity_name|capitalize }} heropenen</h1>
    <p>
        Weet u zeker dat u {{ entity_name }}
        {{ html.link(entity, path('clip_vragen_view', {id: entity.id})) }}
        wilt heropenen?
    </p>
    {{ form(form) }}
{% endblock %}
