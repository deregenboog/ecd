{% include('hs_subnavigation.twig') %}

<h1>Groep verwijderen</h1>
<p>
    Weet u zeker dat u groep <strong>{{ oekGroep }}</strong> wilt verwijderen?<br>
    {{ form_start(form) }}
        {{ html.link('Nee, terug', {'action': 'index'})|raw }}
        <button type="submit">Ja, verwijderen</button>
    {{ form_end(form) }}
</p>
