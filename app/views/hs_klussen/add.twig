{% include('hs_subnavigation.twig') %}

<h1>Klus toevoegen voor {{ hsKlant.klant }}</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Opslaan" />
{{ form_end(form) }}